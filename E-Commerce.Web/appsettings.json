{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "DefaultConnection": "", // Set in User Secrets (dev) or Environment Variables (prod)
        "IdentityConnection": "", // Set in User Secrets (dev) or Environment Variables (prod)
        "RedisConnection": "" // Set in User Secrets (dev) or Environment Variables (prod)
    },
    "BaseUrl": "", // Set via Environment Variables in production

    "Stripe": {
        "SecretKey": "", // Set via User Secrets or Environment Variables
        "EndPointSecret": "" // Set via User Secrets or Environment Variables
    }
}